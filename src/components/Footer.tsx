import styles from "./Footer.module.css"

export default function Footer() {
    return (
        <footer className={styles.footer}>
            Created by Aliz, not associated with Discord Inc.
        </footer>
    )
}